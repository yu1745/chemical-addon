# 11 · 开发计划与内容量（Create 架构版）

> 文档状态：**current**（2026-08 依代码现状重排精简；**§2 是 M3+ 开工顺序的唯一定义**）
> 重排依据：`docs/progress.md`（基线，64/64 GameTest）+ 源码核查（ReactorControllerBlockEntity / ReactorTank / RulesEngine / Solution / build.gradle）。
> 旧版按特性域排（电化学→索尔维→高压→零排放）且含已完成的前置项（D18），已废；流态范式（原 13-flow-modes，已并入 03 §13.4）的 F1/F2/F3 对应本序 U5/U8/U9。已完成里程碑明细以 `docs/progress.md` 为准，本文件不再重复。

## 1. 内容量统计（目标口径，v2）

| 类别 | 数量 | 说明 |
|------|------|------|
| 化学物种（v2 口径，见 03/08） | **61** | 纯流体 15（13 气体 + 热媒 + 水=原版）/ 溶液浆料模式别名（species JSON 21 份）/ 固体 item 18 / 矿物 5 |
| 化学反应 | **52** | 其中 ~34 釜内 chemical_reaction、~18 Create 原生配方（物理过程） |
| 自研方块 | **~35–39** | 模板结构件 ~13 + 专用功能块 ~12 + 辅助小件（S02–S15，S01 已取消）~14 |
| 复用方块 | 大量 | Create 管道/罐/泵/阀/搅拌/热源/物流/电网 + 创想全套 |
| 新矿物 | 5 | 岩盐/石灰石/硫磺/铝土矿/磷矿 |
| 物品（固体产品/催化剂/工具） | ~25 | 盐、纯碱、滤饼、催化剂、化学品罐等 |
| 阶段 | 5（T0–T4） | 见 [archive/02-progression.md](archive/02-progression.md)（冻结） |
| 机制 | 10 | 见 05-mechanics |
| 单人主流程 | 40–60h | 全成就 80–120h |

## 2. 开发顺序（M3+，按依赖链重排）

**排序原则**：公共地基优先（U1 一个单元修完四条线共用的堵点）；廉价硬前置提前（U2）；高风险大块尽早启动（U3 模板抽取、U8 连续流）；每单元独立可验证（`./gradlew build` + 既有 64 GameTest 零回归 + 新增测试）；内容并行（C 轨道）。**开发主线仍是索尔维 spine（D8）**：U4→U6→U7→U8→U9 即 spine 本身（旧 §2.2 索尔维硬件清单已由单元序列 + [04-machines](04-machines.md) 覆盖，删除）。

> **已完成单元（U1/U3/U13–U18）的明细与验收以 `docs/progress.md` 为唯一定义**（2026-08 精简：本表历史长行已压缩为一行索引，避免双家同步）。

### 2.0 基线缺口（排序依据，代码证据）

| # | 缺口（代码证据） | 堵住的下游 |
|---|------|-----------|
| G1 | 多相釜不加热：`updateHeat()` 见 `fluids.size()!=1` 即返回——D18 后气体是永久旁观相，液+气共存是常态而非瞬态（ReactorControllerBlockEntity ~L296） | 一切气液容器（吸收/氯碱/碳化）、压力模型 |
| G2 | 放热只打最重液相（deltaHeat→`getFluids().get(0)` L540）；温度 per-stack NBT 而非容器级（`getTemperature()` L1225） | 相变/冷凝、连续流、蒸馏塔 |
| G3 | 压力/搅拌/催化字段为零（源码 grep 无命中） | M2 压力、S03 压力表、合成氨、催化 |
| G4 | build.gradle 无 createaddition 依赖（D16 电力路线未接线） | 电解槽、电热器 |
| G5 | 结构逻辑（四面×W×H×环层校验/绑定/扩展/收缩/洒漏）内联在 1429 行控制器；沉淀池是另写的拷贝；塔式内部件与 `INTERIOR_BLOCKED` 校验冲突 | M04/M05/M15/M16 全部塔式实例 |
| G6 | `completeRecipe()` 对每个 ingredient 固定消耗 1 个——批式语义内嵌（L475 起） | 连续流比例消耗 |
| G7 | 内容欠账：物种 JSON 21/61、chemical_reaction 配方 4/~34、矿物 0/5、datagen/JEI 未接 | 全部单元（C 轨道并行补） |

### 2.1 单元序列

| 单元 | 内容（修/建） | 依赖 | 估时 | 验收（新增 GameTest 要点） |
|------|------|------|------|------|
| **U1 容器状态层** | 修 G1/G2/G3：`updateHeat` 删单相 early-return、全相按量加权松弛；deltaHeat 作用全体；釜级 `pressure` 字段（闭口 P=k×气量×T 线性简化，开口恒常压）+ NBT + HUD；S03 压力表（照 S02 双形态范本：IMasterBound 墙块+薄板、服务端求解同步、ScrollValueBehaviour 粗粒度阈值、比较器）；温度窗口速率系数（搅拌系数占位 1.0） | 无（首先开工） | 1–1.5 周 | 多相加热两相都松弛；放热全体升温；闭口充气升压/开口恒常压；S03 读数/报警/比较器 |
| **U2 FE 接线** | 修 G4：build.gradle 照 Jade 先例加 createaddition（`fg.deobf`，版本对齐生产 1.3.3）；dev 服务端冒烟 | 无 | 0.5 天（插 U1 间隙） | build + run-server 通过；FE capability 可解析 |
| **U3 模板抽取** | 修 G5：结构层（几何校验/绑定 IMasterBound/生命周期/洒漏/能力代理）抽 `vessel/`；釜纯迁移；沉淀池 SmartBlockEntity 化删拷贝；`INTERIOR_BLOCKED` 放宽为内部件 allowlist（**默认空**）；控制器拆类（结构/状态/引擎层，单文件 <600 行） | 建议在 U1 合入后 | 1.5–2 周 | 64 测试零回归；allowlist 内腔可成型；沉淀池行为等价 |
| **U4 竖窑 M16** | 首个塔式实例：塔式形状参数（1×1~3×3 底×高 3–12）；连续进出料（传送带/漏斗）；石灰石→CaO+CO₂（SUPERHEATED）、重碱煅烧→Na₂CO₃+CO₂+H₂O | U3 | 1–1.5 周 | 成型/两条煅烧/belt 进石灰石端到端 |
| **U5 F1 批式节拍** | S11 液位计、S12 反应状态接口（ReactorStatus+进度→红石）、S13 计量投料（脉冲定量 N mB）、S15 四相程控器（相时长滚动调值，外部红石互通）、S04 波美计（照 S02 模式，读密度非物种——U17 重定义口径） | 无硬依赖（排 U4 后） | 2 周 | 满/空检测、完成态红石、定量注入精度、四相循环、密度阈值 |
| **U6 吸收塔 M05** | 喷淋头/填料注册进 allowlist；气液逆流简化为共存吸收（速率×填料数）；盐水吸氨配方（放热，依赖 U1 多相温度） | U4+U1 | 1.5–2 周 | 含内部件成型；速率随填料变化；氨盐水浓度区间 |
| **U7 蒸馏塔 M04** | 塔板+底部再沸+塔顶冷凝；规则引擎第四条 **volatilise**（沸点查表拆挥发性溶质为气相，冷凝为逆）；蒸氨走白名单配方 | U6+U1 | 2 周 | 氨水受热塔顶出氨、冷凝回液、蒸氨端到端 |
| **U8 F2 连续流** | 修 G6：容器 `mode` 字段（BATCH/CONTINUOUS）+`applyContinuous()`（τ=V/Q 分率转化，Q=fill 记账滑动平均；**比例消耗**，不复用批式 completeRecipe）；产物+未反应随流出；沉淀连续入 Suspended；S14 溢流堰（物理自稳，无红石）；连续模式诊断态（稳态/扰动/排空） | U1（建议 U6 后） | 2–2.5 周 | 稳态液位；τ→转化率曲线（量化离散误差）；未反应气循环；连续析出 |
| **U9 索尔维闭环** | 碳化塔连续模式：氨盐水+CO₂（U4 窑气）→NaHCO₃ 入 Suspended→过滤（M2 已有）→煅烧（U4）→蒸氨（U7）+石灰乳；全链调平 | U4+U6+U7+U8 | 1.5–2 周 | 碳化连续出浆端到端 + 实机全链闭环（C3 矿物须先完成） |
| **U10 电解槽 M09** | FE 耗电（createaddition `BaseElectricBlockEntity` 模式）+电极；盐水电解→NaOH 液+Cl₂+H₂（三产物分离口）、水电解；HCl 合成（点火禁忌=事故表首条）；去离子水首版=原版水（杂质判定层后置） | U1+U2（**可提前**至 U5 后并行） | 1.5 周 | 耗电电解、三产物分离、HCl 合成 |
| **U11 高压线** | M12 压缩机（旋转→气体增压）；M03 高压釜+材质分级落地（化工砖/钢壳/合金壳，10 §2.3）；S05 泄压阀+超压温和爆炸（SpillLogic+原版爆炸 API，配置化）；合成氨（高压+催化，转化率上限 0.15–0.2→循环复用 U8）；S07 催化托盘+接触法 SO₃（V₂O₅ 降热级门槛） | U1+U8 | 2–2.5 周 | 增压、超压爆炸、转化率上限+循环、催化降门槛 |
| **U12 收尾** | M08 蒸发结晶器+CaCl₂ 收尾；M13 换热器（两流股温度交换）；M15 净化塔；禁忌组合检测入规则引擎安全网（D2/D17）+不互溶共管混液炸管对接（M9）；搅拌头（kinetic）可选；M14/M11/M17 弹性 | U9+U10（U11 弹性） | 2 周 | 各一件测试 + 零排放实机验收 |
| **U13 规则引擎 v2**（插队 ✅） | equilibria 统一条目 + 质量作用求解器 + 投料溶解/蒸发浓缩 + speciation 诊断 | 无 | 1 周 | ✅ 明细见 docs/progress.md §U13 |
| **U14 JUnit 引擎测试层 + 常用无机材料矩阵**（插队 ✅） | composition 层剥离 MC（`./gradlew test`）+ 两旋钮制 + 弱电解质通道 + 引擎数据层 21 物种 | U13 | 3 天 | ✅ 明细见 docs/progress.md §U14 |
| **U15 晶粒、投种与混合固体物品**（插队 ✅） | 晶粒 1/16 面额 + 投种塌缩亚稳 + mixed_residue 整坨取出 + 苦卤盐曲线 | U13/U14 | 1 周 | ✅ 明细见 docs/progress.md §U15 |
| **U16 反应热能量记账**（插队 ✅） | J/unit 账本 + ΔT=Q/(feedUnits×c) 质量反比 + 蒸发潜热自限 | U13/U14 | 2–3 天 | ✅ 明细见 docs/progress.md §U16 |
| **U16.5 湿饼夹带与洗涤**（插队 ✅） | 残液率夹带 + residue 母液相 + 再浆/置换洗涤两路 + S18 电导率计 | U15 | 3 天 | ✅ 明细见 docs/progress.md §U16.5 |
| **U17 分析化学层 + 终点控制**（插队 ✅） | S16 pH/S04 波美/S17 浊度 + 试纸族 7 件 + SI 降级 + M08 终点结晶器 | U15 | 1.5–2 周 | ✅ 明细见 docs/progress.md §U17 |
| **U18 定点分数**（插队 ✅） | 求解刻度量子化 10⁷/mB：Mixture long 通道 + 引擎量子往返 + 分块协同移动 | — | — | ✅ 明细见 docs/progress.md §U18（焓记账后继 U19 见本文件附录） |

**关键语义与回退**：
- **U1**：温度**存储**维持 per-FluidStack NBT（运输正确性已验证，不动），只把**作用面**扩到全相；密封本单元只做开/闭二值，材质分级耐压留 U11。
- **U3**：纯迁移不改行为（64 测试即安全网）；allowlist 默认空；拆类是唯一窗口，拖过 U6 后每个塔式 +700 行变体。
- **U7**：蒸氨走白名单配方（NH₄Cl 液+石灰乳+热→NH₃ 气+CaCl₂ 液）——「挥发性弱碱形成」规则化后置，不停主线。
- **U8**：全计划最高风险（结算语义重写）；先 GameTest 量化 τ 离散误差；回退=「小批高频」批式近似连续。
- **U9**：NaHCO₃ 析出走 equilibria 沉淀条目（质量作用、部分析出留母液）进 Suspended，数据显式选边；引擎边界（红氧归配方层）见 03 §8.1。
- **U11**：压力沿用 Temperature 式 NBT 属性，**不注册「高压气体」新流体**（复用 mixture ratio-tag 已验证的身份模式）。

**内容并行轨道（不占关键路径）**：C1 物种 21→61（`tools/gen_species.py` 单一数据源）；C2 配方 4→~34（物理过程优先 Create 原生 Mixing/Crushing 不进釜，06 §7）；C3 矿物 5 + worldgen（**U9 实机验收前必须**）；C4 datagen（U4 后任意）；C5 JEI+Ponder（U5 后任意）；C6 纹理升级（任意）。
**远期弹性（未排期，独立系统）**：萃取（液-液分配）——复用 equilibria 条目形状（`(o)` 相后缀）但需要多液相联立求解 + 「溶在油里」存储语义 + 独立萃取器容器（搅拌=界面传质），不与釜内水溶液反应同釜执行（03 §12 决策）；**Kw 条目**（纯水自电离/pH 7，分辨率门 1e-7 已留，建模决策后置到 pH 玩法立项）；**rate 字段现役数据授权**（机制就绪零数据，M08 蒸发结晶器/U8 连续流时逐条加）；**底排口零头打包晶粒**（U15 的后置钩子：<1000 mB 沉底余量可选打包为晶粒吐出，解决出料零头锁死）。

### 2.2 排程与里程碑映射

单人兼职串行 U1→U12 约 **21–22 周 ≈ 5–5.5 人·月**（与旧估算总量相当，但把旧表隐含在「索尔维」行内的地基/模板/节拍显式化为独立单元）。弹性：U2 半天插 U1 间隙；U10 可提前至 U5 后（依赖最少且给玩家早期内容）；U1/U3 顺序可换但建议 U1 先合（改动面小，避免大冲突）。对外里程碑口径（[archive/02-progression.md](archive/02-progression.md)，冻结）：U1–U5=地基与批式自动化；U6–U9=索尔维闭环（T2 旗舰）；U10=电化学；U11=高压/合成氨（T3）；U12=零排放（T3+/T4）。**开发顺序≠玩家解锁顺序**。

### 2.3 每单元完成定义（DoD）

1. `./gradlew build` 通过；2. `runGameTestServer` 全绿（零回归+新增）；3. `docs/progress.md` 增补条目（**自本计划起按 U 编号记录**）；4. 新机制动手前先读参考实现（AGENTS 表：多方块/能力代理→Create，电机器→createaddition，数据驱动组合→匠魂；新参考源追加进 AGENTS 表）；5. 世界内交互四条自检（仪表不 GUI/诊断可表达/控制物理化/例外仅三类）；6. 涉及平衡的配方实机跑一遍产线再收尾。

## 3. 工作量估算

| 里程碑 | 兼职工作量 | 主要风险 |
|--------|-------------------|----------|
| M0–M2.5 + v2 子系统（规则引擎/离子基底 S1–S3g/D18/S02） | ✅ 已完成（明细见 progress.md） | — |
| U1–U5 地基与批式自动化 | ~6–7.5 周 | 温度全体化改变既有热平衡；模板抽取回归 |
| U6–U9 索尔维闭环 | ~7–8.5 周 | 连续流引擎（U8） |
| U10 电化学 | ~1.5 周 | 电解三产物分离 |
| U11 高压/合成氨 | ~2–2.5 周 | 压力/密封/爆炸调平 |
| U12 收尾/零排放 | ~2 周 | 净化塔、平衡性 |
| **M3+ 合计** | **~21–22 兼职周 ≈ 5–5.5 人·月** | 单人兼职约 5–6 个月（含测试） |

## 4. 风险清单

| 风险 | 等级 | 缓解 |
|------|------|------|
| Create API 变动（6.0.8 锁定，不追新） | 低 | 锁定版本 |
| 温度全体化改变既有热平衡（U1） | 中 | 全量 GameTest + 硫酸线实机复跑 |
| 模板抽取大重构回归（U3） | 中 | 纯迁移不改行为；64 测试安全网；git 可回退 |
| 巨石控制器膨胀（U3 是唯一窗口） | 中 | 拆类拖过 U6 后每塔式 +700 行变体 |
| τ 离散误差不可接受（U8，13 §10） | 中 | GameTest 先量化；回退「小批高频」批式近似 |
| 连续流结算重写超支（U8） | 中 | 估时含 30–50% 缓冲；必要时 S14 后置先交引擎 |
| createaddition 客户端 refmap 崩溃（U2） | 低 | 服务端验证为主，客户端回退不加（AGENTS Jade 先例） |
| 釜内模拟性能（多容器同区块） | 低-中 | 低频结算 + 同区块调度器合并（10 §7，已预留） |
| 高压事故平衡（U11） | 中 | 配置化默认温和；FTB Chunks 在服白拿 |
| 贴图/模型工作量（v2 后溶液不注册） | 低-中 | 程序化生成（gen_species.py） |
| KubeJS 对自定义 RecipeType 支持深度 | 低 | datagen+JEI 先行，脚本化是红利 |

---

## 附录 · U19 烠记账规格（原 13-enthalpy-ledger 全文并入，2026-08）

> 状态：**计划**（U18 定点分数已落地，本单元是其直接后继）。前置：U13（equilibria 统一条目）、U16（J/unit 账本）、U18（量子网格 10⁷/mB）。

### 动机：退休最后两条热护栏

引擎里现在有两条与热相关的**尺度护栏**（`Solution.java`），它们的存在不是因为化学，而是因为**平衡移动没有热记账**——同一个弱电解质条目在被强酸环境「倒转」时，引擎无法区分「真平衡」与「账没记」，只能一刀切冻结：

| 护栏 | 位置 | 行为 | 为什么要退休 |
|------|------|------|--------------|
| **强酸冻结** | `equilibrate()` 中 `hasTerm(eq.right(), H) && bulkAcid()` → `continue` | H⁺ 宏观（> feed×`STRONG_ION_FRACTION`≈pH 4）时，产酸条目（如 NH₄⁺ 水解）整体不参与松弛 | 真实体系中水解照样进行（只是被 Le Chatelier 压到痕量）；冻结把它变成全有/全无开关。`STRONG_ION_FRACTION` 本身也要删 |
| **体相中和阈值** | `neutralise()`/`neutraliseDirect` 的 both-bulk 守卫 + `driveWeakElectrolytes` 的 bulkAcid 守卫 | 只有 H⁺/OH⁻ 都「宏观」时才允许绕过逐对配对直接体相中和 | 它防止弱电解质自产 H⁺/OH⁻ 被「免费」中和、每轮泵出 `NEUTRALISATION_J_PER_PAIR` 的假热。烠记账后热随每次 `applyMove` 走，守卫失去存在理由 |

退休后的正确行为：**所有条目始终参与平衡**；任何方向、任何耦合的移动都按 ΔH 记热；Le Chatelier 效应由 van't Hoff 温度耦合（可选）或至少由热账本的 ΔT 自然表达。

### 机制：每条目 ΔH，记在每一次移动上

`Equilibrium` 已解析并保留 `delta_h`（kJ/mol，`Equilibrium.java` 的 `deltaH` 字段，现 v1 未用）。U19 启用它：

1. **记账点下沉到 `applyMove`**：它是所有平衡移动的唯一入口（松弛 `solveEntry`、协同移动 `coupleDeficits`、中和 `neutralise` 全走它）。在这里累加 `energyJ += m × deltaH(eq) × J_PER_EVENT`（符号随 m），就不存在「哪条路径漏了记账」——强酸冻结当年防的「静默质子化不记账」从结构上消失。中和的 `NEUTRALISATION_J_PER_PAIR` 改为水条目 `H2O = H+1 + OH-1` 的 delta_h（补 ΔH≈+57.3 kJ/mol 即正向离解吸热、中和放热），删掉特例。
2. **配方层不改**：红氧/热解/电化学仍是配方层（03 §8.1 边界），配方的 `deltaHeat` 走 U16 既有通道，两边在 `energyJ` 汇合。
3. **数据表**：给全部既有 equilibria 条目补 `delta_h`（真实值，来源与 log_k 同口径）。缺省 NaN = 0 J（无热效应），引擎不因缺数据卡住。
4. **（可选，验收后评估）van't Hoff**：`effectiveLogK` 已是所有亲和度计算的单点，加 `logK(T) = logK(T0) − ΔH/(2.303R) × (1/T − 1/T0)` 即可让温度真正移动平衡（结晶冷却曲线从查表变涌现）。先做记账、后做耦合，两步分开验收。

### 验收（DoD）

- JUnit：删护栏后既有 66 用例 + 新增全绿：强酸中 NH₄⁺ 水解条目参与计算且被压到痕量（不是开关式消失）；滴定/缓冲、掩蔽、malachite、能量账本用例数值不变（或按新账本更准）。
- GameTest 102 全绿；性能：`applyMove` 每次一次乘加，不新增循环。
- 文档：`Chemistry`/`Solution` 头注释更新；plans/03 §8 引用本附录。

### 风险

- **数据量**：~30 条 equilibria 要查 ΔH；先覆盖教学主线（索尔维七条+三酸两碱+水解/络合），其余渐进。
- **双重记账**：`applyMove` 记账后，`neutralise()` 特例必须同 PR 删除，否则热翻倍（测试立刻暴露——单入口账本的意义）。
- **van't Hoff 与溶解度曲线并存**：曲线是查表结晶（U14 动力学），van't Hoff 只作用于 equilibria 条目，作用域不重叠。